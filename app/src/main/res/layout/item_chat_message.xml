<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="4dp">

    <!-- Bulle de message utilisateur -->
    <LinearLayout
        android:id="@+id/userBubble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_marginLeft="64dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/user_message_background"
        android:padding="12dp">

        <TextView
            android:id="@+id/textViewUserMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:text="Message utilisateur" />

    </LinearLayout>

    <!-- Bulle de message bot -->
    <LinearLayout
        android:id="@+id/botBubble"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="start"
        android:layout_marginRight="64dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/bot_message_background"
        android:padding="12dp">

        <TextView
            android:id="@+id/textViewBotMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#333333"
            android:textSize="14sp"
            android:text="Message du bot" />

    </LinearLayout>

</LinearLayout>
